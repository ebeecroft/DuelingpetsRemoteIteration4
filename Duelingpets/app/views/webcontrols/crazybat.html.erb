<% provide(:h1, "Missingpage") %>
<% if current_user %>
   <p class="pageformat"><%= playMusicLoop("Missing", nil) %></p>
   <div class="pageformat">
      <% if current_user.userinfo.mute_on  %>
         <p><%= button_to 'Unmute music', muteAudio_path(:pageType => "Missing"), method: :post, class: "button submitbutton" %></p>
      <% else %>
         <p><%= button_to 'Mute music', muteAudio_path(:pageType => "Missing"), method: :post, class: "button submitbutton" %></p>
      <% end %>
   </div>
<% end %>
<div class="pageformat">
   <p>Hmm that is strange the page you were looking for appears to have vanished off the face of the earth.</p>
   <p>However not to worry I found something most pecular that might tickle your fancy that is if I can find it.</p>
   <p>Let see now where did I put that thing?</p>
   <p>Ahh here it is I finally found the page you were looking for all along.</p>
</div>
<br>
<p class="formlinks"><%= link_to 'Back', root_path %></p>
